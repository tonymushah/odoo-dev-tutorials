<?xml version="1.0" ?>
<odoo>
<record id="estate_property_offer_view_list" model="ir.ui.view">
    <field name="name">estate.property.offer.list</field>
    <field name="model">estate.property.offer</field>
    <field name="arch" type="xml">
        <list
                string="Property Offers"
                editable="top"
                decoration-success="status == 'accepted'"
                decoration-danger="status == 'refused'"
            >
            <field name="price" string="Price" />
            <field name="buyer_partner_id" string="Partner" />
            <field name="validity" string="Validity (days)" />
            <field name="date_deadline" string="Deadline" />
            <button
                    name="accept_offer_action"
                    type="object"
                    invisible="property_id.state != 'new' and property_id.state != 'offer_received'"
                    icon="fa-check"
                />
            <button
                    name="refuse_offer_action"
                    type="object"
                    invisible="property_id.state != 'new' and property_id.state != 'offer_received'"
                    icon="fa-close"
                />
            <field name="status" string="Status" />
        </list>
    </field>
</record>
<record id="estate_property_offer_offer" model="ir.ui.view">
    <field name="name">estate.property.offer.form</field>
    <field name="model">estate.property.offer</field>
    <field name="arch" type="xml">
        <form string="Property Offer">
            <sheet>
                <group>
                    <field name="price" string="Price" />
                    <field
                            name="buyer_partner_id"
                            string="Partner"
                            required="true"
                        />
                    <field name="validity" string="Validity (days)" />
                    <field name="date_deadline" string="Deadline" />
                    <!-- <field name="status" string="Status" /> -->
                    <!-- <field name="property_id" string="Property" required="true"/> -->
                </group>
            </sheet>
        </form>
    </field>
</record>
</odoo>
